# Managed Git worktrees

Worktree checkouts live at `.worktrees/<project_id>/<short-purpose-slug>/` and are ignored.
Read `docs/repository/PROJECT_STRUCTURE_CODEX.md` before creating or removing one. Register
each auxiliary worktree in `registry.csv`; do not list the primary checkout.
