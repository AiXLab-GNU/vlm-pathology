# Project-owned model resources

Each subdirectory names the scientific project that owns the local model workspace. Shared
consumption requires an explicit immutable manifest; it does not transfer scientific claims.
