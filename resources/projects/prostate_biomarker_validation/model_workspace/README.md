# Prostate biomarker legacy model workspace

This project-owned local workspace contains pretrained weights, third-party model code,
pinned worker environments, embedding caches, and stability-run products from the original
biomarker workspace. It is ignored except for this policy file. PNI and quantitative studies
may consume only hash-registered immutable assets; they do not own or mutate this workspace.
