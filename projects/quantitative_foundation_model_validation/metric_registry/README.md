# Medical quantitative-metric registry

The tier taxonomy is defined in `docs/metric_taxonomy/`. FM1 eligibility tables are the
study-local executable registry; the shared web visualization is built by
`packages/vlm_pathology_metrics`.
