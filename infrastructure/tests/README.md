# Repository integration tests

These tests validate boundaries shared by all studies, including canonical project
roots, compatibility links, immutable-source checks, and the project-scoped Git worktree
registry. Scientific unit tests stay inside the project that owns the analysis.
